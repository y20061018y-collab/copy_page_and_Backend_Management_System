# Triage Labels

Use these canonical labels for local issues:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

The project spec issue created from the confirmed requirements is marked `ready-for-agent` and does not require additional triage.
