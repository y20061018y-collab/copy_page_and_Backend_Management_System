"use client";

import { FormEvent, useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";

type Service = { id: number; name: string; price: string; description: string; sort_order: number; is_active: boolean };
type Game = { id: number; name: string; slug: string; tag: string; description: string; cover_image: string; accent_color: string; accent_color_2: string; sort_order: number; is_active: boolean; services: Service[] };
const emptyService = { name: "", price: "", description: "", sort_order: 0, is_active: true };

export default function EditGame() {
  const params = useParams<{ id: string }>(); const router = useRouter(); const [game, setGame] = useState<Game | null>(null); const [service, setService] = useState(emptyService); const [dirty, setDirty] = useState(false); const [message, setMessage] = useState("");
  const load = () => fetch("/api/admin/games").then((response) => response.json()).then((games: Game[]) => setGame(games.find((item) => String(item.id) === params.id) ?? null));
  useEffect(() => { void load(); }, [params.id]);
  useEffect(() => { const warn = (event: BeforeUnloadEvent) => { if (dirty) { event.preventDefault(); event.returnValue = ""; } }; window.addEventListener("beforeunload", warn); return () => window.removeEventListener("beforeunload", warn); }, [dirty]);
  if (!game) return <main className="admin-shell"><section className="dashboard"><h1>加载中...</h1></section></main>;
  const saveGame = async (event: FormEvent) => { event.preventDefault(); setMessage("保存中..."); const response = await fetch(`/api/admin/games/${game.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(game) }); setMessage(response.ok ? "已保存" : "保存失败"); setDirty(false); };
  const addService = async (event: FormEvent) => { event.preventDefault(); await fetch(`/api/admin/games/${game.id}/services`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(service) }); setService(emptyService); load(); };
  const toggleService = async (item: Service) => { await fetch(`/api/admin/services/${item.id}/${item.is_active ? "disable" : "enable"}`, { method: "POST" }); load(); };
  return <main className="admin-shell"><nav className="admin-nav"><strong>11号电竞后台</strong><a href="/admin">概览</a><a href="/admin/games">游戏管理</a><a href="/admin/settings">网站设置</a></nav><section className="dashboard"><p className="eyebrow">GAME EDITOR</p><h1>{game.name}</h1><form className="settings-form" onSubmit={saveGame}><label>游戏名称<input value={game.name} onChange={(event) => { setGame({ ...game, name: event.target.value }); setDirty(true); }} /></label><label>Slug<input value={game.slug} onChange={(event) => { setGame({ ...game, slug: event.target.value }); setDirty(true); }} /></label><label>标签<input value={game.tag} onChange={(event) => { setGame({ ...game, tag: event.target.value }); setDirty(true); }} /></label><label>简介<textarea value={game.description} onChange={(event) => { setGame({ ...game, description: event.target.value }); setDirty(true); }} /></label><label>排序值<input type="number" value={game.sort_order} onChange={(event) => { setGame({ ...game, sort_order: Number(event.target.value) }); setDirty(true); }} /></label><div><button className="admin-action">保存游戏</button><span className="save-message">{message}</span></div></form><h2 className="subheading">服务列表</h2><div className="admin-game-list">{game.services.map((item) => <article key={item.id}><div><strong>{item.name}</strong><span>{item.price} · {item.description}</span></div><button onClick={() => toggleService(item)}>{item.is_active ? "禁用" : "恢复"}</button></article>)}</div><form className="service-add" onSubmit={addService}><h2>新增服务</h2><input placeholder="服务名称" value={service.name} onChange={(event) => setService({ ...service, name: event.target.value })} required /><input placeholder="价格" value={service.price} onChange={(event) => setService({ ...service, price: event.target.value })} required /><input placeholder="说明" value={service.description} onChange={(event) => setService({ ...service, description: event.target.value })} /><button className="admin-action">添加服务</button></form><button className="back-link" onClick={() => router.push("/admin/games")}>返回游戏列表</button></section></main>;
}
